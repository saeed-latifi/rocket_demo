must define DATABASE_URL in .env file for postgresql
replace libintel-9 file in target folder after first run  : this  older version c library have some issue in creating db pool in runtime and its not any rust issue
